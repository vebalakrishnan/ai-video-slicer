[tool.ruff]
line-length = 100
target-version = "py311"

[tool.ruff.lint]
select = ["E", "F", "B", "ASYNC", "RUF"]
ignore = [
    "B008", # FastAPI's `Depends()`/`Query()`/etc. in argument defaults is the idiomatic DI pattern, not a bug.
]
